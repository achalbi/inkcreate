test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

gcs:
  service: GCS
  project: <%= ENV["GCP_PROJECT_ID"] %>
  bucket: <%= ENV["GCS_UPLOAD_BUCKET"] %>
  iam: true
