<section class="workspace-header mb-4">
  <div class="d-flex flex-column flex-lg-row align-items-lg-start justify-content-between gap-3">
    <div class="min-w-0">
      <% if eyebrow.present? %>
        <div class="text-uppercase text-secondary fw-semibold small"><%= eyebrow.to_s %></div>
      <% end %>
      <h1 class="fs-3 mb-1"><%= title %></h1>
      <p class="text-secondary mb-0 workspace-page-lead"><%= description %></p>
    </div>

    <% if local_assigns[:action_path].present? && local_assigns[:action_label].present? %>
      <div class="d-flex flex-shrink-0">
        <%= link_to action_label, action_path, class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>

  <div class="mt-3">
    <%= render "shared/flash_banners" %>
  </div>
</section>
